field:
  name: Status
  description:
    Current issue status
  type: select
  required: true
  default: "{{ record['status'] or row['status'] or 'Open'}}"
  data:
      options:
        - Open
        - Closed
