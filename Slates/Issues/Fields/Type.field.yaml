field:
  name: Issue Type
  type: select
  required: true
  default: "{{ record['type'] or row['type'] or 'Issue'}}"
  data:
      options:
        - Bug
        - Documentation
        - Enhancement
        - Help Wanted
        - Question
        - Issue

